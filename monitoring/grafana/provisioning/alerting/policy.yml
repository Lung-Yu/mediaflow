apiVersion: 1
policies:
  - orgId: 1
    receiver: MediaFlow Webhook
    routes:
      - receiver: MediaFlow Webhook
        matchers:
          - severity =~ "critical|warning"
        continue: true
      - receiver: MediaFlow Email
        matchers:
          - severity = critical
