-r requirements.txt
# CVE-2025-71176 (pytest tmpdir, medium): fixed at 9.0.3 which requires Python >=3.10.
# CI uses Python 3.11 so it gets the safe version. Host (Python 3.9) stays at 8.4.2.
pytest==8.4.2; python_version < "3.10"
pytest>=9.0.3; python_version >= "3.10"
pytest-asyncio==1.4.0
